<nav class="navbar navbar-expand-lg navbar-dark bg-white mt-5">
    <div class="container"  style="width: 900px;">
      <a class="navbar-brand text-secondary" href="/"><h3>NO CAT, NO LIFE</h3></a>
      
        <div class="navbar justify-content-end">
        <% if logged_in? %>
          <ul class="navbar-nav">
            <li class="nav-item"><%= link_to 'マイページ', user_path(current_user), class: 'nav-link text-secondary' %></li>
            <li class="nav-item"><%= link_to 'ログアウト', logout_path, method: :delete, class: 'nav-link text-secondary' %></li>
          </ul>
        <% else %>
          <ul class="navbar-nav">
            <li class="nav-item"><%= link_to '会員登録', signup_path, class: 'nav-link text-secondary' %></li>
            <li class="nav-item"><%= link_to 'ログイン', login_path, class: 'nav-link text-secondary' %></li>
          </ul>
        <% end %>
      </div>
    </div>
</nav>
